package _03_FP

import java.util.*

/**
 * Refaça sua calculadora usando lambdas, generics e scope functions.
 * Fora as 4 operações básicas, acrescente o operador unário fib
 * Para simplificar considere a ordem de precedência da esquerda para direita.
 * Ex: 1 + 2 + 5 - 4 / 2 * 2 = (((((1 + 2) + 5) - 4) / 2) * 2) = 4
 * Ex: 1 + fib10 + fib6 + 1 / 5 = ((((1 + (fib10)) + (fib6)) + 1) / 5) = 13
 */
fun main(args: Array<String>) {
    val scan = Scanner(System.`in`)
}
