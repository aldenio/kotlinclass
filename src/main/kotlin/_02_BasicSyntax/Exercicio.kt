package _02_BasicSyntax

import util.next

/**
 * Faça uma calculadora que lê o input do teclado e devole o resultado.
 * Use: when, try, if e loops.
 * A calculadora deve resolver expressões com as 4 operações básicas. Considerando números positivos e negativos
 *
 *
 * Para simplificar considere a ordem de precedência da esquerda para direita.
 * Ex: 1 + 2 + 5 - 4 / 2 * 2 = (((((1 + 2) + 5) - 4) / 2) * 2) = 4
 * Ex: -1 - -2 - 3 = -1 + 2 + -3 = ((-1 + 2) + -3) = -2
 * A calculadora deve utilizar só a função next() para ler o input.
 */
fun main(args: Array<String>) {
    while (true) {
        println(next())
    }
}


